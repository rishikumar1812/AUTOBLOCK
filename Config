# Network settings
MAIN_PC_IP = "192.168.1.100"   # Change to actual Main PC IP
PORT = 5000

# Paths
BASE_LOG_DIR = "logs"
REGISTER_DIR = "register"
STATE_FILE = "state.json"

# Thresholds
FAIL_COUNT_THRESHOLD = 3
FAIL_RATE_THRESHOLD = 0.5  # percent

# DL list
DL_LIST = [f"DL{str(i).zfill(2)}" for i in range(1, 21)]